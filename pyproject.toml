[project]
name = "ruby"
version = "0.1.0"
dependencies = [
    "flet>=0.25.0",
    "google-genai",
    "playwright",
    "requests",
    "beautifulsoup4",
    "websockets"
]
requires-python = ">=3.12"
